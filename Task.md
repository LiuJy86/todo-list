# 待办事项清单 — 任务拆解（Task.md）

> 本文件基于 `.claude/requirement.md` 的需求分析与 `.claude/CLAUDE.md` 的项目约束生成。
> 所有相关资料均放置于 `Claude_ToDoList_2_workshop\` 目录下。

---

## 一、需求分析

### 1. 原始需求

来源：`.claude/requirement.md`

1. 页面上可以输入一条待办事项
2. 点击「添加」后，事项会显示在列表中
3. 每条事项可以：
   - 标记为已完成
   - 删除
4. 页面刷新后，数据存在 session

### 2. 需求拆解为功能点

| 编号 | 功能点 | 说明 | 对应需求 |
| :-- | :-- | :-- | :-- |
| F1 | 输入待办 | 提供输入框，用户可输入一条事项文本 | 需求 1 |
| F2 | 添加到列表 | 点击「添加」按钮后，将输入的文本作为一条事项加入列表 | 需求 2 |
| F3 | 标记完成 | 每条事项可切换「已完成 / 未完成」状态，已完成时视觉上有所区分 | 需求 3 |
| F4 | 删除事项 | 每条事项可从列表中移除 | 需求 3 |
| F5 | 会话持久化 | 页面刷新后数据不丢失，存储于 `sessionStorage`（关闭标签页即清空） | 需求 4 |
| F6 | 完成项自动沉底 | 勾选完成后该事项移至列表末尾，未勾选事项按原顺序向上冒泡 | 增强需求 |
| F7 | 收纳与展开 | 已完成项超过 3 条自动收纳，支持展开/收起切换 | 增强需求 |
| F8 | 无闪动更新 | 使用 DOM 节点缓存实现增量更新，操作流畅无闪动 | 增强需求 |
| F9 | 未完成项收纳 | 未完成（待办中）事项超过 5 条时自动收纳，与已完成收纳共用一个展开开关 | 增强需求 5 |
| F10 | 点击空白召唤桌宠 | 点击页面空白处让史迪奇走到点击位置 | 增强需求 6 |
| F11 | 桌宠自动避障 | 史迪奇移动时自动避开功能按钮（按钮、输入框、待办项等） | 增强需求 6 |
| F12 | 待办操作 toast 反馈 | 添加/完成/删除待办时显示文字 toast，配合史迪奇情绪 | 增强需求 6 |

### 3. 隐含/补充说明

- **存储方式选择**：需求中「存在 session」明确指向 `sessionStorage`（而非 `localStorage`）。区别在于 `sessionStorage` 仅在当前标签页会话内有效，关闭标签页后清空，更贴合「session」语义。
- **空输入处理**：用户未输入内容就点击「添加」时，不应添加空事项（隐含的健壮性要求）。
- **状态视觉反馈**：标记为已完成的事项需有明显的视觉区分（如删除线、置灰），让用户一眼可辨。
- **数据结构**：每条事项至少包含三个字段——`id`（唯一标识）、`text`（内容）、`done`（是否完成）。

---

## 二、技术方案

### 1. 技术栈（遵循 `.claude/CLAUDE.md` 约束）

- **HTML**：页面结构
- **CSS**：样式（单文件，无框架）
- **JavaScript**：交互逻辑（原生 JS，无 React/Vue）
- **存储**：浏览器内置 `sessionStorage` API
- **无后端、无数据库、无构建工具**

### 2. 文件结构

```
Claude_ToDoList_2_workshop\
├── .claude\
│   ├── CLAUDE.md          # 项目说明与约束
│   ├── requirement.md     # 原始需求
│   └── settings.json      # 配置
├── Task.md                # 本文件：任务拆解
├── index.html             # 页面结构（任务 1 创建）
├── style.css              # 样式（任务 2 创建）
└── script.js              # 交互逻辑（任务 3~6 创建）
```

### 3. UI 设计原则（结合用户偏好）

- 内容在一屏内完整展示，不出现滚动条
- 表单分组清晰：输入区（输入框 + 添加按钮）与列表区分明
- 视觉简洁，去除多余留白与装饰
- 交互流畅，无布局抖动

---

## 三、任务拆解

> 遵循 `.claude/CLAUDE.md`：「每次只完成一个任务」「先解释思路，再给代码」。
> 以下 6 个任务按依赖顺序排列，建议逐个完成。

### 任务 1：搭建 HTML 页面结构

- **目标**：创建 `index.html`，搭建页面的骨架结构。
- **包含元素**：
  - 页面标题
  - 输入区：一个文本输入框 + 一个「添加」按钮
  - 列表区：一个空的 `<ul>` 容器，用于承载待办事项
  - 引入 `style.css` 与 `script.js`
- **思路要点**：用语义化标签（`<header>`、`<main>`、`<section>`）组织结构，便于后续样式与脚本操作。
- **验收标准**：浏览器打开 `index.html` 能看到标题、输入框、按钮和空列表区域。
- **产出文件**：`index.html`

### 任务 2：编写 CSS 样式

- **目标**：创建 `style.css`，让页面整洁美观且一屏展示。
- **包含内容**：
  - 全局重置（margin/padding 归零，统一字体）
  - 页面整体居中、限定最大宽度
  - 输入区布局（输入框与按钮横向排列、间距合理）
  - 列表项样式（每条事项一行、内部按钮右对齐）
  - 「已完成」状态的视觉区分（删除线 + 置灰）
  - 保证内容在常见分辨率下不出现滚动条
- **思路要点**：用 Flexbox 布局输入区与列表项内部结构，简单直观。
- **验收标准**：页面样式整洁，输入框、按钮、列表项布局合理，无明显多余空白。
- **产出文件**：`style.css`

### 任务 3：实现「添加」功能

- **目标**：在 `script.js` 中实现输入并添加待办事项到列表。
- **包含逻辑**：
  - 监听「添加」按钮的点击事件
  - 读取输入框的值，去除首尾空格
  - 空内容不添加（可加简单提示）
  - 创建列表项 DOM 元素并追加到 `<ul>`
  - 添加后清空输入框并聚焦，方便连续输入
- **思路要点**：用一个数组 `todos` 作为数据源，渲染列表时根据数组生成 DOM，便于后续与存储对接。
- **验收标准**：输入文字点击「添加」后，列表中立刻出现对应事项；空输入不生效。
- **产出文件**：`script.js`（追加）

### 任务 4：实现「标记为已完成」功能

- **目标**：每条事项可切换完成状态。
- **包含逻辑**：
  - 每条事项前添加一个复选框（或点击文本切换）
  - 点击切换该条事项的 `done` 字段
  - 根据状态为事项添加/移除「已完成」样式类（删除线、置灰）
  - 更新数组中对应数据
- **思路要点**：通过事项的 `id` 定位要修改的数组项，避免直接操作 DOM 导致数据与视图不一致。
- **验收标准**：点击复选框后，事项样式在「正常 / 已完成」之间正确切换。
- **产出文件**：`script.js`（追加）

### 任务 5：实现「删除」功能

- **目标**：每条事项可从列表中删除。
- **包含逻辑**：
  - 每条事项右侧添加一个「删除」按钮
  - 点击后从数组中移除对应项（按 `id` 定位）
  - 重新渲染列表
- **思路要点**：删除即从数据源数组中过滤掉对应项，再统一渲染，逻辑简单不易出错。
- **验收标准**：点击「删除」后，对应事项从列表中消失，其他事项不受影响。
- **产出文件**：`script.js`（追加）

### 任务 6：实现 sessionStorage 持久化

- **目标**：页面刷新后数据不丢失，存储于 `sessionStorage`。
- **包含逻辑**：
  - 封装 `save()`：将 `todos` 数组序列化为 JSON 存入 `sessionStorage`
  - 封装 `load()`：页面加载时从 `sessionStorage` 读取并反序列化
  - 在「添加 / 标记完成 / 删除」三个操作后均调用 `save()`
  - 页面初始化时调用 `load()`，若存在历史数据则渲染到列表
- **思路要点**：`sessionStorage` 的键名固定（如 `todos`），值的格式为 JSON 字符串。所有数据变更后统一调用 `save()`，避免遗漏。
- **验收标准**：
  - 添加若干事项后刷新页面，列表依然显示这些事项及其完成状态
  - 关闭标签页后重新打开，列表为空（session 语义验证）
- **产出文件**：`script.js`（追加）

### 任务 7：实现完成项自动沉底

- **目标**：勾选完成后将事项移至列表末尾，未勾选事项按原顺序向上冒泡，使待办列表始终保持「未完成在上、已完成在下」的清晰结构。
- **包含逻辑**：
  - 重写 `toggleTodo(id)`：翻转 `done` 字段后，按规则重新排序 `todos` 数组
  - **勾选完成（done: false → true）**：将该事项从原位置移除并追加到数组末尾（已完成区末尾）
  - **取消勾选（done: true → false）**：将该事项插入到「所有未完成事项之后、已完成事项之前」，保持未完成组按原添加顺序向上冒泡
  - 排序基于数据数组本身，渲染时由 `render()` 自然反映新顺序，无需额外 DOM 操作
- **思路要点**：使用 `filter` 移除目标项后，按 `done` 状态选择插入位置：
  - 勾选 → `array.push(todo)` 追加到末尾
  - 取消勾选 → 用 `findIndex` 找到第一个已完成项的位置，用 `splice` 插入其前
- **验收标准**：
  - 添加 3 条事项 A、B、C，勾选 B 后列表顺序为 A、C、B(已完成)
  - 再勾选 A 后列表顺序为 C、B(已完成)、A(已完成)（新勾选项追加到已完成区末尾）
  - 取消勾选 A 后列表顺序为 C、A、B(已完成)，未完成组保持原添加顺序
  - 列表顺序持久化到 `sessionStorage`，刷新后保持一致
- **产出文件**：`script.js`（重写 `toggleTodo`）

### 任务 8：实现收纳与展开功能

- **目标**：当已完成项超过指定阈值时自动收纳，用户可展开/收起收纳的事项。
- **包含逻辑**：
  - 定义 `VISIBLE_COMPLETED_LIMIT = 3` 常量作为收纳阈值
  - 当已完成项数量超过阈值时，超出部分移到收纳容器
  - 显示「还有 N 项已完成待办 ▼」提示按钮
  - 点击按钮切换展开/收起状态
  - 展开时按钮变为「收起 ▲」，收纳容器显示所有已完成项
- **思路要点**：使用 `isExpanded` 状态标志控制收纳区显示，`render()` 函数根据状态动态调整 DOM 结构。
- **验收标准**：
  - 勾选 4 条事项后，第 4 条被收纳
  - 收纳按钮正确显示收纳数量
  - 点击展开后所有已完成项可见
  - 点击收起后恢复收纳状态
- **产出文件**：`script.js`（修改 `render()`）、`style.css`（新增收纳样式）

### 任务 8.1：实现未完成项收纳功能

- **目标**：当未完成（待办中）事项超过指定阈值时也自动收纳，与已完成收纳共用一个展开开关。
- **包含逻辑**：
  - 新增 `VISIBLE_PENDING_LIMIT = 5` 常量作为未完成项收纳阈值（待办是主要关注内容，阈值略高于已完成项）
  - 未完成项超过 5 条时，超出部分移入收纳容器
  - 新增「还有 N 项待办 ▼」收纳按钮（蓝绿色样式，与已完成收纳按钮的紫色区分）
  - 两类收纳共用 `isExpanded` 状态，点击任一按钮都可同时展开/收起
  - 展开时收纳容器同时显示未完成和已完成的被收纳项
- **思路要点**：
  - `render()` 重构为「未完成可见项 → 未完成收纳按钮 → 已完成可见项 → 已完成收纳按钮 → 收纳容器」的渲染顺序
  - 收纳按钮位置计算需考虑前一个按钮的占位偏移
  - 使用 `collapse-btn-pending` CSS 类区分两类按钮的视觉样式
- **验收标准**：
  - 添加 6 条以上未完成事项后，超出 5 条的部分被收纳
  - 未完成收纳按钮正确显示「还有 N 项待办」
  - 点击未完成收纳按钮可展开所有被收纳项（包括已完成的）
  - 展开后按钮文案变为「收起 ▲」
  - 未完成和已完成收纳按钮颜色有明显区分
- **产出文件**：`script.js`（重写 `render()` 收纳逻辑）、`style.css`（新增 `.collapse-btn-pending` 样式）

### 任务 9：实现 DOM 节点缓存与无闪动更新

- **目标**：重构渲染逻辑，使用节点缓存实现增量更新，避免全量重建导致的闪动。
- **包含逻辑**：
  - 使用 `Map` 创建 `nodeCache` 缓存已创建的 `<li>` 节点
  - `render()` 函数从「清空重建」改为「增量更新」模式
  - 通过 `insertBefore` 移动节点位置，而非 `innerHTML = ''` 清空
  - 勾选/取消勾选时仅切换 `classList` 和 `checkbox.checked`
  - 添加新项时创建新节点并加入缓存
  - 删除项时从缓存中移除节点
- **思路要点**：节点以 `todo.id` 为键缓存，确保节点可复用。操作时通过 DOM API 移动节点，浏览器不会产生闪动。
- **验收标准**：
  - 勾选/取消勾选时界面无闪动
  - 添加/删除事项时操作流畅
  - 节点 ID 稳定，不频繁重建
- **产出文件**：`script.js`（重构 `render()`、新增 `createTodoElement`、`updateItemState`）

### 任务 12：视觉与交互优化（v1.8.0）

- **目标**：优化桌宠动效、收纳按钮对比度，并将独立 toast 系统改为史迪奇对话气泡。
- **包含逻辑**：
  - **桌宠动效**：将 `index.html` 中 `<img>` 的 `src` 从 `史迪奇.png` 改为 `史迪奇.gif`，让桌宠呈现动态效果
  - **收纳按钮对比度优化**：`.collapse-btn` 加深背景色、边框加粗至 `1.5px`、新增 `box-shadow` 投影；hover/active 状态加深阴影；未完成按钮 `.collapse-btn-pending` 用深蓝绿色 `#1e6e6f` 区分
  - **toast 改为对话气泡**：移除页面顶部独立 toast 系统（DOM + CSS），所有待办操作反馈通过史迪奇对话气泡 `pet-bubble` 展示
  - `showBubble(customMsg)` 扩展支持自定义文案（4 秒显示）
  - `showPetToast(text, type)` 复用气泡，按 `type-success/info/warning/error` 切换气泡颜色，并触发弹跳动效
  - `window.petMood.toast` 别名到 `showPetToast`，保持接口不变
- **思路要点**：统一反馈通道——所有文字反馈都让史迪奇「说话」，减少 DOM 元素数量，增强角色感与一致性。
- **验收标准**：
  - 桌宠显示为 GIF 动图，有动态效果
  - 收纳按钮有清晰边框、阴影与层次感，未完成/已完成按钮颜色区分明显
  - 添加待办时史迪奇弹出绿色边框气泡显示文案
  - 完成待办时弹出绿色庆祝气泡
  - 删除待办时根据状态弹出 info/warning 气泡
  - 页面顶部无独立 toast 提示条残留
- **产出文件**：`index.html`、`script.js`、`style.css`

### 任务 13：桌宠 GIF 多图轮播（v1.9.0）

- **目标**：将桌宠图片改为 `img/` 文件夹下的多张 GIF，每 3-5 秒随机轮播切换。
- **包含逻辑**：
  - **图片路径迁移**：`<img>` 的 `src` 从根目录 `史迪奇.gif` 改为 `img/史迪奇.gif`，资源统一收纳到 `img/` 文件夹
  - **GIF 列表**：`PET_GIFS` 常量数组包含 7 张 gif（史迪奇系列 6 张 + 线条小狗 1 张）
  - **随机轮播**：`scheduleNextGif()` 以 `3000 + Math.random() * 2000` ms（3-5 秒）随机间隔递归调度，仿 `scheduleNextMove` 模式
  - **不重复**：随机选下一张时排除当前下标，保证每次切换都有变化
  - **预加载 + 淡入淡出**：`switchToNextGif()` 用 `new Image()` 预加载下一张，`onload` 后先淡出（0.3s）换 `src` 再淡入（0.3s），避免闪白
  - **生命周期集成**：`hidePet()` 停止轮播；恢复按钮重启；`visibilitychange` 页面不可见时停止、可见时重启；启动区调用启动
- **思路要点**：复用现有定时器调度模式（`scheduleNextMove`/`startRandomMovement`/`stopRandomMovement`），保持代码风格一致；预加载避免切换时闪白。
- **验收标准**：
  - 桌宠初始显示 `img/史迪奇.gif`
  - 等待后 `src` 在 7 张 gif 之间随机切换
  - 切换间隔约 3-5 秒
  - 切换时有淡入淡出效果，无闪白
  - 隐藏/恢复桌宠时轮播正确停止/重启
  - 页面切到后台时轮播停止，回到前台时恢复
- **产出文件**：`index.html`、`script.js`

### 任务 14：桌宠边界融入优化（v2.0.0）

- **目标**：修正图片路径 404 bug，并优化桌宠图片边界融入，使其自然浮于页面而非贴片。
- **包含逻辑**：
  - **路径 bug 修复**：经 Python `os.listdir` 核实，img 文件夹实际文件名为 `史迪奇1.gif`~`史迪奇6.gif`（共 6 张），此前代码引用的 `史迪奇.gif` 与 `线条小狗.gif` 不存在
    - `index.html`：`<img>` 的 `src` 改为 `img/史迪奇1.gif`
    - `script.js`：`PET_GIFS` 改为 6 张实际文件名，移除不存在的两条
  - **边缘羽化（mask）**：`.pet-img` 增加径向 `mask` 渐变（`radial-gradient(ellipse 97% 97% at 50% 48%, #000 80%, rgba(0,0,0,0.55) 90%, transparent 100%)`），让 GIF 四周自然淡出，消除硬矩形边界
  - **柔和双层投影**：`drop-shadow` 改为双层（底层 `0 10px 14px` 大范围漫射 + 顶层 `0 3px 4px` 近距离接触），让桌宠立体浮于页面
  - **放大平滑**：`image-rendering: auto`，弱化 90×90 原图放大到 140px 的像素感
  - **统一过渡**：CSS `transition` 增加 `opacity 0.3s ease`；移除 `switchToNextGif` 中的 inline `transition` 设置，避免覆盖 transform 动画
- **思路要点**：用 mask 径向渐变在图片边缘 80%→100% 范围羽化淡出，既消除硬边界又不切掉史迪奇主体；双层投影营造立体悬浮感。
- **验收标准**：
  - 桌宠初始显示 `img/史迪奇1.gif`，无 404
  - 轮播在 6 张 gif 间切换，均正常加载
  - 图片四周边缘自然淡出，无明显硬矩形边界
  - 桌宠有柔和投影，立体浮于页面
  - 切换时淡入淡出正常，控制台无报错
- **产出文件**：`index.html`、`script.js`、`style.css`

---

## 四、整体验收标准（最终交付）

完成后，应用需满足以下全部条件：

1. ✅ 页面有输入框，可输入一条待办事项
2. ✅ 点击「添加」后，事项显示在列表中
3. ✅ 每条事项可标记为已完成（有视觉区分）
4. ✅ 每条事项可删除
5. ✅ 刷新页面后，事项数据仍存在于 `sessionStorage` 中
6. ✅ 代码仅使用 HTML / CSS / JavaScript，无框架、无后端
7. ✅ 关键代码均有中文注释，思路清晰，适合无开发经验者阅读
8. ✅ 页面在常见分辨率下无滚动条，布局整洁无抖动
9. ✅ 勾选完成后该事项自动移至列表末尾，未勾选事项按原顺序向上冒泡
10. ✅ 已完成项超过 3 条时自动收纳，支持展开/收起
11. ✅ 操作流畅无闪动，DOM 节点复用
12. ✅ 未完成项超过 5 条时自动收纳，与已完成收纳共用展开开关
13. ✅ 点击空白处召唤史迪奇走到点击位置
14. ✅ 史迪奇移动时自动避开功能按钮
15. ✅ 添加/完成/删除待办时显示文字 toast
16. ✅ 桌宠显示为 GIF 动图，有动态效果
17. ✅ 收纳按钮（收拉框）有清晰边框、阴影与层次感，对比度足够
18. ✅ 待办操作反馈通过史迪奇对话气泡展示（无独立 toast 提示条），按类型切换气泡颜色
19. ✅ 桌宠图片取自 `img/` 文件夹，3-5 秒随机轮播切换多张 GIF，切换有淡入淡出
20. ✅ 桌宠图片路径正确无 404，图片边界经 mask 羽化自然融入背景，柔和投影立体浮于页面

---

## 五、任务进度追踪

| 任务 | 名称 | 状态 | 产出文件 |
| :--: | :-- | :--: | :-- |
| 1 | 搭建 HTML 页面结构 | ✅ 已完成 | `index.html` |
| 2 | 编写 CSS 样式 | ✅ 已完成 | `style.css` |
| 3 | 实现「添加」功能 | ✅ 已完成 | `script.js` |
| 4 | 实现「标记为已完成」功能 | ✅ 已完成 | `script.js` |
| 5 | 实现「删除」功能 | ✅ 已完成 | `script.js` |
| 6 | 实现 sessionStorage 持久化 | ✅ 已完成 | `script.js` |
| 7 | 实现完成项自动沉底 | ✅ 已完成 | `script.js` |
| 8 | 实现收纳与展开功能 | ✅ 已完成 | `script.js`、`style.css` |
| 8.1 | 实现未完成项收纳功能 | ✅ 已完成 | `script.js`、`style.css` |
| 9 | 实现 DOM 节点缓存与无闪动更新 | ✅ 已完成 | `script.js` |
| 10 | 实现点击空白召唤桌宠 + 自动避障 | ✅ 已完成 | `script.js` |
| 11 | 实现待办操作 toast 反馈 | ✅ 已完成 | `script.js`、`style.css` |
| 12 | 视觉与交互优化（GIF 动效 / 收拉框对比度 / toast 改为对话气泡） | ✅ 已完成 | `index.html`、`script.js`、`style.css` |
| 13 | 桌宠 GIF 多图轮播（img 文件夹下 7 张 gif，3-5 秒随机切换） | ✅ 已完成 | `index.html`、`script.js` |
| 14 | 桌宠边界融入优化（修正路径 404 + mask 羽化 + 双层投影） | ✅ 已完成 | `index.html`、`script.js`、`style.css` |

> 状态标记：⬜ 未开始 / 🔄 进行中 / ✅ 已完成
